# Environment Configuration for SecureScribeBE

# CORS Origins (comma-separated URLs)
# For development, you can use: http://localhost:3000,http://localhost:8080
# For production, specify your actual frontend domains
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Database Configuration
POSTGRES_SERVER=160.191.88.194
POSTGRES_PORT=5400
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DB=securescribe

# JWT Configuration (optional - will use auto-generated if not provided)
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=11520
REFRESH_TOKEN_EXPIRE_MINUTES=43200

# Firebase Configuration (path to service account key file)
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=config/keys/scribe-c7f13-firebase-adminsdk-fbsvc-1ab2f55755.json

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# MinIO Configuration
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET_NAME=securescribe-files
MINIO_PUBLIC_BUCKET_NAME=securescribe-public
MINIO_PUBLIC_URL=http://localhost:9000

# Qdrant Configuration
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=documents

# Google AI Configuration
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_EMBEDDING_MODEL=models/embedding-001

# Bot Service Configuration
BOT_SERVICE_URL=http://host.docker.internal:3000

# Server Configuration
SERVER_NAME=SecureScribeBE
SERVER_HOST=http://localhost
SERVER_PORT=8000
